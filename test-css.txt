.test-result { margin-top: 10px; padding: 10px; border-radius: 4px; background: rgba(0,0,0,0.3); font-size: 0.85rem; max-height: 200px; overflow-y: auto; }
        .test-result .success { color: var(--neon-green); }
        .test-result .error { color: var(--neon-red); }
        .log-output { background: rgba(0,0,0,0.5); border: 1px solid var(--border-color); border-radius: 4px; padding: 10px; max-height: 300px; overflow-y: auto; font-family: "Share Tech Mono", monospace; font-size: 0.8rem; }
        .log-output div { margin: 2px 0; padding: 5px; border-bottom: 1px solid rgba(255,255,255,0.05); }
        .log-output .error { color: var(--neon-red); }
        .log-output .success { color: var(--neon-green); }
        .btn-small { padding: 5px 10px; font-size: 0.7rem; background: var(--neon-cyan); color: #000; border: none; border-radius: 3px; cursor: pointer; }